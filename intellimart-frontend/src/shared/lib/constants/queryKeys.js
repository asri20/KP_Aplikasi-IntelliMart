/**
 * React Query Keys
 * Centralized query keys untuk cache management
 */

export const QUERY_KEYS = Object.freeze({
  // Landing
  FEATURES: ['features'],
  PRICING: ['pricing'],
  
  // Auth (placeholder untuk iterasi 2)
  AUTH_USER: ['auth', 'user'],
  
  // Tenant (placeholder untuk iterasi 3)
  TENANT: ['tenant'],
  TENANT_LIST: ['tenant', 'list'],
  
  // Dashboard (placeholder untuk iterasi 3)
  DASHBOARD_STATS: ['dashboard', 'stats'],
});
